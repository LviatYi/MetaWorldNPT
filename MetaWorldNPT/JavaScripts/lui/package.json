{
  "name": "mw-lynx-ui",
  "version": "31.1.1",
  "author": "LviatYi",
  "description": "Lynx Credit By G.S.C.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "./dist/**/*",
    "./pic/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && npm i && rollup --config rollup.config.mjs && rimraf ./node_modules",
    "publish": "npm publish",
    "clean": "rimraf ./dist",
    "build publish": "rimraf ./dist && npm i && rollup --config rollup.config.mjs && rimraf ./node_modules && npm publish",
    "create link": "npm link"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LviatYi/MetaWorldNPT.git"
  },
  "dependencies": {
    "gtoolkit": "^31.15.12",
    "mw-log4ts": "^1.4.0",
    "fuse.js": "^7.0.0",
    "linq": "^4.0.3"
  },
  "devDependencies": {
    "glob": "^10.4.1",
    "rimraf": "^5.0.7",
    "rollup": "^4.18.0",
    "rollup-plugin-flat-dts": "^2.6.0",
    "rollup-plugin-ts": "^3.4.5",
    "typescript": "^5.4.3"
  }
}
